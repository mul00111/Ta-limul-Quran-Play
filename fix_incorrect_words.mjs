import fs from 'fs';

async function findAyah(text) {
  // Let's just fetch from an api or we can use the local file if it exists.
  // Actually, I can use the local file /app/applet/quran_results.txt or something.
}
